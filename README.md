# MicRouter

Per-app microphone routing for rooted Android devices using LSPosed.

Work in progress: configurable input-device routing, Material 3 Expressive UI, and optional per-app software input gain for Java `AudioRecord` capture paths.
